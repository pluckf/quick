# 禽蛋裂纹/正常分类工作说明（基于敲击响应信号）

本文档用于**单独、完整**记录在本项目中完成的工作：从数据读取、特征工程、特征筛选、模型训练与评估，到“创新点”算法优化与结果导出。

- 工作目录：`e:/desk_top/a/quick/`
- 数据目录：`bad/`（裂纹蛋）、`good/`（正常蛋）
- 主实验笔记本：`main.ipynb`
- 环境：Conda 虚拟环境 `machine`（Python 3.11）

---

## 1. 数据理解与格式约定

### 1.1 数据来源

- `good/`：正常蛋样本（每个 `.xlsx` 文件为一个鸡蛋）
- `bad/`：裂纹蛋样本（每个 `.xlsx` 文件为一个鸡蛋）

本次统计（以目录实际文件数为准）：
- 正常蛋：30
- 裂纹蛋：43
- 总样本：73

### 1.2 单个样本的 xlsx 结构

每个 `.xlsx` 文件记录同一个鸡蛋的**敲击响应**，并满足以下约定：

- 采样频率：$f_s = 48\,\text{kHz}$
- 数据为 **4 列波形**，且**没有表头**
- 列含义：
  - 第 1、2 列：**大端敲击**两个通道
  - 第 3、4 列：**小端敲击**两个通道
- 每个样本长度：4800 点（约 0.1 秒）

### 1.3 关键坑点与修复

最初用 `pandas.read_excel()` 默认会把首行当作表头，导致：
- 误把首行数据当列名
- 实际读取变成 4799×4（丢 1 行）

修复方式：读取时显式使用 `header=None`。

---

## 2. 目标与总体方案

### 2.1 目标

对禽蛋进行二分类：
- `0`：正常（good）
- `1`：裂纹（bad）

### 2.2 方案选择（依据样本量）

当前样本量约 73，属于**小样本**场景。

- 直接用深度学习端到端模型（CNN/Transformer）易过拟合、可复现性与解释性也更弱
- 采用“**信号特征工程 + 传统机器学习**”更稳健、易解释、对小样本更友好

总体流程：
1. 读取 xlsx（大端/小端、双通道）
2. 预处理（去直流/零均值）
3. 提取三域特征（时域、频域、时频域）+ 小波能量
4. 特征筛选（单变量互信息 SelectKBest）
5. 模型选择与训练（SVM vs RandomForest）
6. **创新点**：大端/小端多视角加权融合（自动寻优权重）
7. 指标评估与结果导出（CSV、joblib、混淆矩阵图）

---

## 3. 实现细节（对应 main.ipynb）

### 3.1 数据读取

实现了函数逻辑：
- 使用 `pandas.read_excel(..., header=None, engine='openpyxl')`
- 转为 `numpy.ndarray`，并确保取前 4 列
- 返回 shape=(n_samples, 4) 的浮点数组

### 3.2 预处理

对每个通道做零均值：

$$x \leftarrow x - \bar{x}$$

目的：降低传感器直流偏置对统计特征与谱特征的影响。

---

## 4. 特征工程（时域 / 频域 / 时频域）

特征提取以“每端敲击（2 通道）”为单位。

### 4.1 双通道汇总策略

每端有 2 个通道。步骤：
1. 对每个通道提取同构特征（同一套 key）
2. 对两个通道的同名特征做汇总：
   - `meanch`：两通道均值
   - `stdch`：两通道标准差

这样得到每端一套更稳定的特征向量。

> 这里修复过一次 KeyError：最初把 `ch0/ch1` 写进了特征名导致两个通道 key 不一致，汇总时会报错；修复为通道不进 key，改为汇总时再加后缀。

### 4.2 时域特征（Time Domain）

对每个通道提取：
- mean、std
- RMS、max|x|、峰峰值 ptp
- skew（偏度）、kurtosis（峭度）
- 过零率 ZCR
- crest = peak / RMS
- energy = \(\sum x^2\)

### 4.3 频域特征（Frequency Domain）

使用 Welch PSD：`scipy.signal.welch`。

提取：
- 谱质心 spectral centroid
- 谱带宽 spectral bandwidth
- 谱熵 spectral entropy
- 主频 dominant frequency
- 85% rolloff
- 频带能量（积分 PSD），频带划分：
  - 0–500 Hz
  - 500–2000 Hz
  - 2000–5000 Hz
  - 5000–10000 Hz
  - 10000–20000 Hz

### 4.4 时频域特征（Time–Frequency Domain）

采用 STFT：`scipy.signal.stft`，对幅值谱 \(|Z(f,t)|\) 做统计：
- 幅值均值、标准差
- 分频带（同上频带划分）的幅值均值/标准差

### 4.5 小波能量特征（Wavelet Energy）

为了补充冲击信号的多尺度特征，引入 DWT：`PyWavelets`。

- 小波：db4
- 分解层数：最多 4 层（或根据数据长度自适应不超过最大可分解层）
- 特征：各层系数能量 \(E_i = \sum c_i^2\)

### 4.6 三套特征集

最终构造三种特征矩阵用于比较：
- **Big**：仅大端特征
- **Small**：仅小端特征
- **All**：大端+小端拼接

本次实际维度：
- `X_big`: 73×74
- `X_small`: 73×74
- `X_all`: 73×148

---

## 5. 特征筛选

由于特征维度相对样本量偏高，在模型训练管线中引入特征筛选：

- 方法：`SelectKBest(mutual_info_classif)`（互信息单变量筛选）
- `k` 候选：20 / 40 / 60（且不超过总维度，必要时自适应）

SVM 还会额外做标准化：
- `StandardScaler` + `SelectKBest` + `SVC`

RandomForest：
- `SelectKBest` + `RandomForestClassifier`

---

## 6. 模型训练与评估

### 6.1 划分与验证策略

- 训练/测试：分层 8:2（`train_test_split(..., stratify=y, random_state=42)`）
- 训练集内：5 折分层交叉验证（`StratifiedKFold(n_splits=5, shuffle=True, random_state=42)`）

### 6.2 对比模型

对每个特征集（Big/Small/All）都做 GridSearchCV：

1) SVM（RBF）
- `C ∈ {1, 10, 100}`
- `gamma ∈ {scale, auto}`
- `k ∈ {20, 40, 60}`（自适应）

2) RandomForest
- `n_estimators ∈ {200, 400}`
- `max_depth ∈ {None, 10, 20}`
- `k ∈ {20, 40, 60}`（自适应）

评分指标：以 F1 为主（`scoring='f1'`）。

### 6.3 本次运行结果（一次固定随机种子划分）

在本次划分下，输出（测试集）如下：
- Big：RF 测试 F1 = 1.000
- Small：RF 测试 F1 = 0.947
- All：RF 测试 F1 = 1.000

> 说明：这是一次固定随机种子的结果；论文/报告更建议补充重复分层 K 折或多次随机划分取均值±方差。

---

## 7. 创新点：两端敲击多视角加权融合（优化点）

### 7.1 动机

大端与小端敲击反映的结构响应差异可能不同：
- 某些裂纹特征在大端更明显
- 某些裂纹特征在小端更明显

因此提出利用“两端信息互补”的融合策略。

### 7.2 方法

分别训练两个概率输出模型：
- 大端模型：在 `X_big` 上训练
- 小端模型：在 `X_small` 上训练

融合概率：

$$p = \alpha\,p_{big} + (1-\alpha)\,p_{small},\quad \alpha\in[0,1]$$

阈值：`p >= 0.5` 判为裂纹。

### 7.3 权重自动寻优（核心优化）

在训练集上用 5 折交叉验证生成 out-of-fold 概率：
- `prob_big_cv`
- `prob_small_cv`

遍历 `alpha ∈ {0,0.1,...,1.0}`，选择使训练集 F1 最大的 alpha。

本次得到：
- `alpha = 0.60`

### 7.4 融合结果

本次划分测试集（15 个样本）融合结果：
- Accuracy = 1.0
- F1 = 1.0
- 混淆矩阵：good 6/6 正确；bad 9/9 正确

---

## 8. 指标说明

二分类常用指标：
- Accuracy：\(\frac{TP+TN}{TP+TN+FP+FN}\)
- Precision：\(\frac{TP}{TP+FP}\)
- Recall：\(\frac{TP}{TP+FN}\)
- F1：\(\frac{2PR}{P+R}\)

本项目训练与搜索中主要使用 F1（更关注裂纹识别的综合表现）。

---

## 9. 可复现产出与文件说明

在 `outputs/` 下已生成：

1) 特征表（可用于论文分析/画图/二次建模）
- `features_big.csv`
- `features_small.csv`
- `features_all.csv`

三者都包含：
- `id`：样本来源（good/xxx.xlsx 或 bad/xxx.xlsx）
- `y`：标签（0 正常，1 裂纹）

2) 模型文件（joblib）
- `model_best_all.joblib`：拼接特征（All）的最佳单模型
- `model_fusion_bundle.joblib`：融合所需组件（big_model、small_model、alpha、特征列等）

3) 可视化
- `confusion_matrix_fusion.png`：融合模型的测试集混淆矩阵图

---

## 10. 如何复现（最简步骤）

1. 激活环境：
   - `conda activate machine`
2. 打开并运行：
   - `main.ipynb` 从上到下依次运行
3. 输出文件将生成到：
   - `outputs/`

---

## 11. 后续可扩展方向（可选）

为使结果更符合论文/开题“稳健性”表达，可考虑：
- 使用 `RepeatedStratifiedKFold` 或多次随机划分，报告均值±标准差
- 引入更强的特征选择（如 RFECV、L1 Logistic、Permutation Importance）
- 设计更贴合冲击信号的特征（包络谱、衰减常数、Q 因子、共振峰统计等）
- 对融合策略做更细粒度优化（阈值联动、概率校准 CalibratedClassifierCV 等）
